# Tested: Python 3.10 on macOS arm64 (MPS) and Linux x86_64 (CUDA).
# Versions below match the env we used to verify end-to-end.

# Core
numpy>=1.26,<2
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
tqdm
soundfile
librosa>=0.10

# PyTorch (pin to a torch version known to work with both laion-clap and
# timm/transformers/sentence-transformers wheels we install below).
# On a CUDA box, install the matching CUDA wheels manually.
torch==2.4.1
torchaudio==2.4.1
torchvision==0.19.1

# LAION-AI CLAP (audio-text contrastive baseline)
laion-clap==1.1.6
ftfy
braceexpand
webdataset
torchlibrosa
h5py
regex

# Transformers / text encoders
transformers>=4.40
tokenizers
huggingface_hub

# M2D-CLAP runtime (used by src/portable_m2d.py)
timm>=1.0
einops
nnAudio
sentence-transformers

# Notebooks
jupyter
ipywidgets
